apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: watcher-binding
  labels:
    app: watcher
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: watcher-role
subjects:
  - kind: ServiceAccount
    name: watcher
    namespace: dep-manager
