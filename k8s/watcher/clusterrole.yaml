apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: watcher-role
  labels:
    app: watcher
rules:
  # Permissions needed for deployment informer (watches deployments cluster-wide)
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "list", "watch"]
