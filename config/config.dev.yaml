server:
  port: "8080"
  shutdown_timeout: 30s

database:
  host: "localhost"
  port: "5432"
  user: "postgres"
  password: "postgres"
  db_name: "k8s_deployment_manager"

nats:
  url: "nats://localhost:4222"
